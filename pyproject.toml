[build-system]
build-backend = "poetry.masonry.api"
requires = ["poetry>=0.12"]

[tool]
[tool.poetry]
name = "conference-registration-web-application"
version = "0.1.0"
description = "Веб-приложение на Django для регистрации участников конференции"
authors = ["Viktoria Chernysheva"]
[tool.poetry.dependencies]
Django = "^3.0"
python = "^3.8"
[tool.poetry.dev-dependencies]
